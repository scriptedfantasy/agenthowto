import type {ReactNode} from 'react';
function inline(value:string):ReactNode[]{const parts=value.split(/(`[^`]+`|\[[^\]]+\]\((?:https?:\/\/|\/)[^\s)]+\))/g);return parts.map((p,i)=>{if(p.startsWith('`')&&p.endsWith('`'))return <code key={i}>{p.slice(1,-1)}</code>;const m=p.match(/^\[([^\]]+)\]\(([^)]+)\)$/);if(m)return <a key={i} href={m[2]} rel="noreferrer noopener">{m[1]}</a>;return p;});}
export function Prose({text,skipTitle=false}:{text:string;skipTitle?:boolean}){const lines=text.split('\n');const blocks:ReactNode[]=[];let code:string[]|null=null;let paragraph:string[]=[];const flush=()=>{if(paragraph.length){blocks.push(<p key={blocks.length}>{inline(paragraph.join(' '))}</p>);paragraph=[];}};for(let i=0;i<lines.length;i++){const line=lines[i];if(/^(?:```|~~~)/.test(line)){flush();if(code){blocks.push(<pre key={blocks.length}><code>{code.join('\n')}</code></pre>);code=null;}else code=[];continue;}if(code){code.push(line);continue;}if(!line.trim()){flush();continue;}if(line.startsWith('# ')){flush();if(!skipTitle)blocks.push(<h1 key={blocks.length}>{inline(line.slice(2))}</h1>);continue;}if(line.startsWith('## ')){flush();const label=line.slice(3);blocks.push(<h2 key={blocks.length} id={label.toLowerCase().replace(/[^a-z0-9]+/g,'-')}>{inline(label)}</h2>);continue;}if(/^\d+\. |^- /.test(line)){flush();const numbered=/^\d/.test(line);const items:string[]=[];while(i<lines.length&&(numbered?/^\d+\. /:/^- /).test(lines[i]))items.push(lines[i++].replace(/^(?:\d+\.|-) /,''));i--;blocks.push(numbered?<ol key={blocks.length}>{items.map((s,j)=><li key={j}>{inline(s)}</li>)}</ol>:<ul key={blocks.length}>{items.map((s,j)=><li key={j}>{inline(s)}</li>)}</ul>);continue;}paragraph.push(line);}flush();if(code)blocks.push(<pre key={blocks.length}>{code.join('\n')}</pre>);return <div className="prose">{blocks}</div>;}
